// তিনটা সংখ্যা এর যোগ করতে পারবে এমন একটা ফাংশন লিখো 

function addThreeNum(number1, number2, number3) {
    var sum = number1 + number2 + number3;
    return sum;
}

console.log(addThreeNum(30, 0.001, 3.12))